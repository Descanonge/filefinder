channels:
  - conda-forge
  - nodefaults
dependencies:
  - python = 3.11

  - ruff
  - mypy>=1.5
  - pytest>=7.4
  - pyfakefs

  - sphinx == 8.0.*
  - sphinx-book-theme == 1.1.*

  - pip
  - pip:
      - hypothesis
      - coverage
      - pytest-cov
